import {Router, Request, Response} from 'express'

const router = Router()

router.get('/city/list', (req: Request, res: Response) => {
  return
})

router.post('/city/create', (req: Request, res: Response) => {
  return
})

router.get('/city/:city_id', (req: Request, res: Response) => {
  return
})

router.patch('/city/:city_id', (req: Request, res: Response) => {
  return
})

router.delete('/city/:city_id', (req: Request, res: Response) => {
  return
})

export default router
